#ifndef _DELAY_H_
#define _DELAY_H_

	#ifndef uchar
	#define uchar unsigned char
	#endif

	#ifndef uint
	#define uint unsigned int
	#endif

//延时函数，延时约为0.01*ims
void delay(uchar i);

#endif
